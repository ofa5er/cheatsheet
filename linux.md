
```bash
ps
awk // example of complex operation
grep
traceroute ping
routes // add a route, delete route, 
iptables // add rules, delete rules
mv rm mkdir ls 
ln / ln -s
find
ifconfig
uname -a
netstat -tunelp
cut // example
cat more less vi vim nano

```
